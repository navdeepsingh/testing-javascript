const username = 'Navdeep Singh'
typeof username === 'string'

if (!('serviceWorker' in navigator)) {
  // you have an old browser
}

const greeting = 'Hello'

console.log(`${greeting}`)
;[1, 2, 3].forEach(x => {
  console.log(x)
})
